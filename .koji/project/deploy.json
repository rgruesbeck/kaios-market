{
  "deploy": {
    "subdomain": ".withkoji.com",
    "frontend": {
      "output": "dist",
      "type": "static",
      "commands": [
        "npm install",
        "npm run build"
      ]
    }
  }
}